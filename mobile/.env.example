# Public values only (they ship inside the app). Same project as the website.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_LIVEKIT_URL=
# Where the API lives. Production for phones; the dev server for the web preview.
EXPO_PUBLIC_SITE_ORIGIN=https://agorasphere.net
