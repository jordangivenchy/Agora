/* Web and Expo Go: no LiveKit, so nobody is heard speaking and there is
   no mic; the seats, hands and roles still work. */
export { useSpeakingIdsFallback as useSpeakingIds, MicButtonFallback as MicButton, ConnectionNoteFallback as ConnectionNote } from "./stageFallback";
