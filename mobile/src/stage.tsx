/* Web and Expo Go: no LiveKit, so nobody is heard speaking and there is
   no mic; the seats, hands and roles still work. */
export { WithSpeakingFallback as WithSpeaking, MicButtonFallback as MicButton, ConnectionNoteFallback as ConnectionNote } from "./stageFallback";
