import type { Metadata } from "next";

export const metadata: Metadata = { title: "Post · AgoraSphere" };

/* One post, in its board. The boards layout renders the page and reads
   the id from the address; see app/(boards)/layout.tsx. */
export default function PostRoute() {
  return null;
}
