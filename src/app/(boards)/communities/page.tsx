import type { Metadata } from "next";

export const metadata: Metadata = { title: "Communities · AgoraSphere" };

/* The boards layout renders the page; see app/(boards)/layout.tsx. */
export default function CommunitiesRoute() {
  return null;
}
