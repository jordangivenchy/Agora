import type { Metadata } from "next";

export const metadata: Metadata = { title: "Communities · AgoraSphere" };

/* One board. The boards layout renders the page and reads the slug from
   the address; see app/(boards)/layout.tsx. */
export default function CommunityRoute() {
  return null;
}
